# Copy to .env and fill in values once the backend/services below are actually deployed.
# None of these are required to run the game today - src/services reads them defensively.

# Base URL for the future backend API (see server/ and src/services/api)
VITE_API_BASE_URL=

# Reserved for future Firebase config (see src/services/firebase)
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_PROJECT_ID=
